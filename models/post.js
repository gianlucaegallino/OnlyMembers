class Post {
    constructor(title, timestamp, content, userid) {
        this.title = title;
        this.timestamp = timestamp;
        this.content = content;
        this.userid = userid;
      }
}

export default {Post};